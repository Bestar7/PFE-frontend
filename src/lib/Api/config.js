const url = "http://localhost";
const port = 9000;
const host = `${url}:${port}`
///pour la PWA :
///npm run preview donc ca c est en local 
///comment faire pour que ce soit en public 
//quest ce qui definit le port
export { host }